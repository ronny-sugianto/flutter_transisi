// Smoke test placeholder — the app uses TransisiApp, not a counter widget.
// Full widget tests are in test/ui/screen/.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(true, isTrue);
  });
}
