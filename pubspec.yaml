name: flutter_transisi
description: "Flutter Test Assessment"

version: 1.0.0+1

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter

  # Router
  go_router: ^17.2.2
  # State Management
  flutter_bloc: ^9.1.1
  # Data
  equatable: ^2.0.8
  # Storage
  shared_preferences: ^2.5.5
  # Network
  http: ^1.6.0
  # UI
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  mockito: ^5.4.4
  build_runner: ^2.4.9
  bloc_test: ^10.0.0

flutter:
  uses-material-design: true
